# Ciclo completo: etiqueta de Git → pipeline → Pull Request → ArgoCD → canary → prod
# Generado el 2026-09-19 08:45:59 CST
#
# La demostración de punta a punta del flujo con la versión 1.2.0. Nadie
# ejecutó kubectl ni helm contra el clúster para desplegar en ningún punto.

## 1 · La etiqueta
   git tag p8-v1.2.0 && git push origin p8-v1.2.0          (08:17 UTC)

## 2 · El pipeline de entrega (6 min 30 s)
   https://github.com/marcosbondel/Practicas-SA-B-202202410/actions/runs/35431569959
   6 imágenes × 2 arquitecturas: construidas, escaneadas con Trivy ANTES de
   publicar, con SBOM, publicadas, firmadas con Cosign y verificadas.
   Ver 13-cadena-de-suministro.txt.

## 3 · El Pull Request que abrió el pipeline
   https://github.com/marcosbondel/sa-gitops-202202410/pull/4
   envs/stage/values.yaml  +5/-5   (una línea por servicio: tag "1.2.0")

## 4 · La revisión humana y el merge   (08:25:25 UTC)

## 5 · El canary de stage, disparado por ArgoCD
   10 % → puerta → 30 % → puerta → 60 % → puerta → 100 % en 412 s.
   Detalle y veredicto de cada puerta en 06-promocion-canary-exitosa.txt.

## 6 · La promoción a prod, SIN reconstruir
   gh workflow run p8-release.yml -f version=1.2.0 -f ambiente=prod
   https://github.com/marcosbondel/Practicas-SA-B-202202410/actions/runs/35447508318  (35 s)
   Comprueba que existe la etiqueta p8-v1.2.0, verifica la firma de las seis
   imágenes que ya están en el registro y abre el PR contra prod:
   https://github.com/marcosbondel/sa-gitops-202202410/pull/5   envs/prod/values.yaml +6/-6
   Lo que llega a producción es, byte a byte, lo que pasó el canary de stage.

## 7 · El canary de prod   (merge 14:04:45 UTC → Healthy 14:12:10 UTC)
   Mismas tres puertas con umbrales más estrictos y el doble de usuarios
   virtuales: p95 14, 12 y 13 ms contra un umbral de 200 ms.

## 8 · Estado final
   Los dos ambientes con 1.2.0, las ocho Applications Synced/Healthy. Ver 08-argocd.txt.
